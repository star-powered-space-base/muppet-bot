# ngrok configuration for Discord bot
version: "2"

# You'll need to add your ngrok authtoken here
# Get it from: https://dashboard.ngrok.com/get-started/your-authtoken
# authtoken: YOUR_NGROK_AUTH_TOKEN_HERE
authtoken: 2aDCK4FspyRervGeWbnl6rPbQPx_3UjnACuPuED112gMEXpc4

tunnels:
  discord-bot:
    # Note: Discord bots typically don't need HTTP tunnels for webhooks
    # since they use Gateway WebSockets. This is for development/testing
    # if you need to expose any HTTP endpoints
    proto: http
    addr: 6666
    #subdomain: discord-bot-persona  # Optional: custom subdomain (requires paid plan)
    inspect: true
    
  # Alternative tunnel for testing different ports
  bot-alt:
    proto: http
    addr: 3000
    inspect: true

# Global configuration
web_addr: 127.0.0.1:4040
console_ui: true
log_level: info
log_format: logfmt
log: ngrok.log

